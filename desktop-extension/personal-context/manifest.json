{
  "manifest_version": "0.1",
  "name": "personal-context",
  "version": "0.2.0",
  "description": "Claude Desktop extension for trusted Markdown personal context: read and search core, dynamic, and inbox memory; build trust-aware task bundles; capture low-trust notes; and promote reviewed notes without editing core files directly.",
  "author": {
    "name": "Abhinav Kalyan"
  },
  "server": {
    "type": "node",
    "entry_point": "server/index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/server/index.js"],
      "env": {
        "PERSONAL_CONTEXT_ROOT": "${user_config.context_root}"
      }
    }
  },
  "user_config": {
    "context_root": {
      "type": "directory",
      "title": "Personal Context Folder",
      "description": "Choose the personal context folder that contains core, dynamic, and inbox.",
      "required": true
    }
  }
}
